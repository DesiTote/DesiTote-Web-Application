# Secrets - never commit. Only .env.example belongs in the repo.
.env
.env.*
!.env.example
*.pem
*.key

# Dependencies / build output
node_modules/
dist/
build/

# Rollback record written by optimizeProductImages.ts — the previous S3 URLs
# for a specific run against a specific database, not shared state.
image-rollback.json

# Logs
*.log
npm-debug.log*
logs/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
