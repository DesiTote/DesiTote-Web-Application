# Base URL of the deployed backend API, with no trailing slash.
# Must use the VITE_ prefix - Vite ignores other prefixes, so an older
# NEXT_PUBLIC_API_URL will not be picked up.
# Vite bakes this in at BUILD time - after changing it on your host, redeploy.
VITE_API_URL=http://localhost:5000
